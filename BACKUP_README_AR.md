# نسخة نقل ونسخ احتياطي لمشروع قرآن القارئ

السكريبت ينشئ أرشيفاً مضغوطاً صالحاً لحفظ المشروع أو نقله إلى جهاز آخر للعمل عليه.

لا يحذف السكريبت أي ملف من المشروع الأصلي ولا ينقل أي ملف منه.

مكان الحفظ الافتراضي يقع خارج المشروع في المجلد التالي:

```text
D:\new project\_backups
```

## ما الذي تحفظه النسخة الافتراضية؟

- جميع ملفات المصدر، وملفات القرآن، والوثائق، ومواد النشر، وأي تعديلات لم تُرسل إلى المستودع.
- كامل مجلد

  `.git`

  بما في ذلك التاريخ والفروع وحالة العمل الحالية.
- مفاتيح توقيع التطبيق وإعداداتها الموجودة في المشروع، لكي يمكن إنتاج إصدار جديد من الجهاز الآخر.

  ```text
  mushaf_app\release.jks
  mushaf_app\keystore.properties
  ```

## ما الذي يستبعده افتراضياً؟

- نواتج البناء والكاش التي يمكن إنشاؤها مجدداً.

  ```text
  build
  .gradle
  .kotlin
  .cxx
  ```

- إعدادات المسارات الخاصة بالجهاز الحالي.

  ```text
  local.properties
  ```

- مجلد العمل القديم ومجلدات أدوات العمل المؤقتة.

  ```text
  old
  .codex
  ```

- ملفات الإصدارات القابلة لإعادة البناء.

  ```text
  .apk
  .aab
  ```

هذا الاستبعاد لا يلمس الملفات الأصلية؛ فقط لا يضعها داخل الأرشيف.

## الاستخدام

نفّذ المعاينة أولاً. ستعرض الحجم المتوقع والاستثناءات من دون إنشاء أي ملف.

```powershell
Set-Location 'D:\new project\quran_01'
.\backup-quran-project.ps1 -Preview
```

بعد مراجعة المعاينة، أنشئ النسخة الفعلية:

```powershell
.\backup-quran-project.ps1
```

للحفظ في قرص خارجي أو مكان آخر:

```powershell
.\backup-quran-project.ps1 -Destination 'E:\Quran-Backups'
```

لإضافة المجلد القديم وملفات الإصدارات أيضاً:

```powershell
.\backup-quran-project.ps1 -IncludeLegacy -IncludeReleaseArtifacts
```

عند إنشاء نسخة يمكن مشاركتها من دون مفاتيح التوقيع أو الإعدادات السرية:

```powershell
.\backup-quran-project.ps1 -ExcludeSecrets
```

## الملفات الناتجة

ينتج السكريبت ثلاثة ملفات متجاورة:

```text
quran_01_YYYY-MM-DD_HH-mm-ss.7z
quran_01_YYYY-MM-DD_HH-mm-ss.7z.sha256
quran_01_YYYY-MM-DD_HH-mm-ss.7z.manifest.json
```

الأول هو الأرشيف.

الثاني بصمة للتحقق من أن النقل لم يتلف الملف.

الثالث تقرير يحتوي تاريخ الإنشاء والحجم والاستثناءات وبيانات التحقق، من دون كلمات مرور.

## التحقق قبل النقل أو بعده

يمكن فحص الأرشيف:

```powershell
7z t 'D:\new project\_backups\quran_01_YYYY-MM-DD_HH-mm-ss.7z'
```

وتحسب البصمة في الجهاز الآخر ثم تطابقها مع الملف المجاور:

```powershell
Get-FileHash 'D:\new project\_backups\quran_01_YYYY-MM-DD_HH-mm-ss.7z' -Algorithm SHA256
Get-Content 'D:\new project\_backups\quran_01_YYYY-MM-DD_HH-mm-ss.7z.sha256'
```

## الاستعادة على جهاز آخر

ثبّت أداة لفك أرشيفات

`7z`

مثل

`NanaZip`

أو

`7-Zip`

ثم فك الأرشيف داخل مجلد العمل الجديد:

```powershell
7z x 'E:\Quran-Backups\quran_01_YYYY-MM-DD_HH-mm-ss.7z' -o'E:\work'
```

سينتج مجلد واحد باسم:

```text
E:\work\quran_01
```

أنشئ ملف إعدادات الجهاز الجديد بعد تثبيت بيئة أندرويد:

```text
mushaf_app\local.properties
```

ويجب أن يشير إلى مكان حزمة تطوير أندرويد في الجهاز الجديد.

بعد ذلك يمكن فتح المشروع وبناؤه بالطريقة المعتادة.

## تنبيه مهم للسلامة

النسخة الافتراضية تحتوي مفتاح توقيع التطبيق وملف إعداداته.

لا ترفعها إلى مستودع عام، ولا ترسلها عبر وسيلة غير موثوقة، واحفظها في قرص مشفر أو مكان خاص.

إذا كان الهدف مشاركة الكود فقط، استخدم الخيار التالي:

```powershell
.\backup-quran-project.ps1 -ExcludeSecrets
```

بيانات الدخول إلى خدمة الاستضافة لا تكون عادة داخل مجلد المشروع؛ ستحتاج إلى تسجيل الدخول إلى حسابك في الجهاز الجديد بشكل منفصل.
